function decodePart(value: string) { const normalized = value.replace(/-/g, "+").replace(/_/g, "/").padEnd(Math.ceil(value.length / 4) * 4, "="); return Uint8Array.from(atob(normalized), (char) => char.charCodeAt(0)); }
async function fromGitHubAction(request: Request) { const token = request.headers.get("authorization")?.replace(/^Bearer\s+/i, ""); if (!token) return false; const parts = token.split("."); if (parts.length !== 3) return false; try { const header = JSON.parse(new TextDecoder().decode(decodePart(parts[0]))) as { alg?: string; kid?: string }; const claims = JSON.parse(new TextDecoder().decode(decodePart(parts[1]))) as Record<string, unknown>; const now = Math.floor(Date.now() / 1000); if (header.alg !== "RS256" || !header.kid || claims.iss !== "https://token.actions.githubusercontent.com" || claims.aud !== "deal-scout" || claims.repository !== "JohnP287/deal-scout" || claims.ref !== "refs/heads/main" || !["schedule", "workflow_dispatch"].includes(String(claims.event_name)) || Number(claims.exp) < now || Number(claims.nbf ?? claims.iat) > now + 30) return false; const response = await fetch("https://token.actions.githubusercontent.com/.well-known/jwks"); if (!response.ok) return false; const jwks = await response.json() as { keys: JsonWebKey[] }; const jwk = jwks.keys.find((key) => key.kid === header.kid); if (!jwk) return false; const key = await crypto.subtle.importKey("jwk", jwk, { name: "RSASSA-PKCS1-v1_5", hash: "SHA-256" }, false, ["verify"]); return crypto.subtle.verify("RSASSA-PKCS1-v1_5", key, decodePart(parts[2]), new TextEncoder().encode(`${parts[0]}.${parts[1]}`)); } catch { return false; } }
export async function scanAuthorized(request: Request) { const origin = request.headers.get("origin"); const sameOrigin = origin && new URL(origin).host === new URL(request.url).host; return Boolean(sameOrigin) || fromGitHubAction(request); }
